#include <stdio.h>
#include <string.h>
#include <stdlib.h>

void str_compare(char* s, int n) {
	
}

int main(void) {
	char str1[50];
	char str2[50];
	int idx1, idx2;

	printf("Input str1: ");
	gets(str1);
	printf("Input str2: ");
	gets(str2);

	idx1 = strlen(str1);
	idx2 = strlen(str2);

	

}
