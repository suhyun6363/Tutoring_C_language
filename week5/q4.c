#include <stdio.h>
#include <string.h>

int main(void) {
	char str[50];
	char lefts[25];
	char rights[25];
	int j = 0;

	size_t size;

	printf("문자열 입력: ");
	gets(str);

	size = strlen(str);
	if(size % 2 != 0) {
		int mid = size / 2;


	}
